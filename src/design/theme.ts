import { buttonShadow, buttonSurface, buttonSurfacePressed, colors, motion, radius, shadow, spacing, typography } from "./tokens";

export const theme = {
  buttonShadow,
  buttonSurface,
  buttonSurfacePressed,
  colors,
  motion,
  radius,
  shadow,
  spacing,
  typography
};

export type AppTheme = typeof theme;
